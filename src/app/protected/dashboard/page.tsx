import useAuth from "@/hooks/useAuth";
import { Navigate } from "react-router-dom";

const Dashboard = () => {
  return (
    <div>
      <h2 className="text-white">Dashboard page (private)</h2>
    </div>
  );
};

export default Dashboard;
